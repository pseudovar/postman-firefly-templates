<h1>Testing</h1>
{{#each response.outputs}}
    <p>
        <h3>Result {{seed}}</h3>
        <img
            src='{{image.presignedUrl}}'
            style='max-width: 100%; max-height: 50vh;'
        />
    </p>
{{/each}}